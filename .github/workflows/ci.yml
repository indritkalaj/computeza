name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel older runs from the same ref when a newer one starts.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# Workflows ship with the minimum required permissions; nothing in CI
# writes to the repo, opens PRs, or pushes packages, so contents: read
# is enough.
permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

# Native cross-platform packaging is a v1.0 commitment (spec section 10),
# so CI must validate Linux + macOS + Windows from day one. Matrix is small
# now to keep CI cheap; expand to ARM64 and the packaging matrix in
# section 10.1 once the packaging machinery lands.

jobs:
  fmt:
    name: rustfmt
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - name: Install Rust toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all --check

  ascii:
    name: ascii-only
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - name: Reject non-ASCII bytes in tracked source files
        # CLAUDE.md rule (the audit on 2026-04-27): source files use only
        # US-ASCII printable + standard whitespace. Em-dashes, section
        # signs, smart quotes, arrows, etc. must be substituted at write
        # time. This guard is a regression net.
        run: |
          set -euo pipefail
          if git grep --cached -nIP '[\x80-\xff]' \
              -- '*.rs' '*.toml' '*.md' '*.ftl' '*.yml' '*.yaml' \
                 '*.json' '*.css' '*.html' '*.lock' \
                 '.gitignore' '.gitattributes' '.editorconfig' \
                 'rust-toolchain.toml' 'rustfmt.toml'; then
            echo
            echo "Non-ASCII characters found in tracked source. Run"
            echo "  python .audit-nonascii.py"
            echo "to see the offenders and substitute them with ASCII"
            echo "equivalents (-- for em-dash, 'section' for section sign,"
            echo "-> for arrows, etc.)."
            exit 1
          fi

  clippy:
    name: clippy
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - name: Install Rust toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --workspace --all-targets --locked

  audit:
    name: cargo audit
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # Advisory only: surfaces known CVEs in the dep graph without blocking
    # the merge. Flip continue-on-error to false when the team is ready
    # to treat advisories as release-blocking.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v5
      - name: Install Rust toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@v2
      - name: Install cargo-audit
        run: cargo install --locked cargo-audit
      - run: cargo audit

  test:
    name: test (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5
      - name: Install Rust toolchain
        run: rustup show
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --workspace --locked
      - run: cargo test --workspace --locked
