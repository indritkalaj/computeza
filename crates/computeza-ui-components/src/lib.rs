//! Computeza UI design system — reusable Leptos components.
//!
//! Per spec §3.2, the design system is a 47-component library; per spec
//! §4.3 every component must conform to the dark-by-default, indigo +
//! orange accent palette. Every component takes localised text via the
//! `computeza-i18n` localizer rather than accepting raw strings.
//!
//! Component categories: layout (page shell, side panels, two-pane
//! diagram-plus-form), navigation (cmd-K palette, breadcrumbs, tabs),
//! data display (resource tables, drift indicators, code tabs), form
//! controls (validated inputs derived from Rust structs via leptos_form),
//! status (badges, alerts, banners), and primitives (buttons, icons via
//! Font Awesome Free, modals).
//!
//! Scaffold stub. Implementation is pending.

#![warn(missing_docs)]
